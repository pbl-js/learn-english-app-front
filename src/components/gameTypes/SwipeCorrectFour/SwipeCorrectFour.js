import React from "react";
import SwipeCorrect from "../SwipeCorrect/SwipeCorrect";

const SwipeCorrectFour = (props) => {
  return <SwipeCorrect {...props} wordsCount={4} />;
};

export default SwipeCorrectFour;
