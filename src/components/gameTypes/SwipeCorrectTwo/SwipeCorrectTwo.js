import React from "react";
import SwipeCorrect from "../SwipeCorrect/SwipeCorrect";

const SwipeCorrectTwo = (props) => {
  return <SwipeCorrect {...props} wordsCount={2} />;
};

export default SwipeCorrectTwo;
